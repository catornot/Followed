name: Build-app

on: [workflow_dispatch]

jobs:
  build:
    name: Build packages
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: macos-latest
            TARGET: macos
            CMD_BUILD1: pyinstaller main.py --noconfirm --onefile --windowed --name "mac-build" &&
            CMD_BUILD2: mv assets dist/build.app/Contents/Resources/assets &&
            CMD_BUILD3: cd dist/ &&
            CMD_BUILD4: zip -r9 mac-build mac-build.app/
            OUT_FILE_NAME: mac-build.zip
          - os: windows-latest
            TARGET: windows
            CMD_BUILD1: pyinstaller main.py --noconfirm --onefile --windowed --name "win-build" &&
            CMD_BUILD2: mv -Path ./assets -Destination ./dist/assets &&
            CMD_BUILD3: Compress-Archive -Path './dist' -DestinationPath './dist/win-zip.zip'
            CMD_BUILD4: echo "e"
            OUT_FILE_NAME: win-build.zip
    steps:
    - uses: actions/checkout@v3
    - name: Set up Python 3.10.2
      uses: actions/setup-python@v3
      with:
        python-version: 3.10.2
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller
        pip install pygame
    - name: Build with pyinstaller for ${{matrix.TARGET}}
      run: ${{matrix.CMD_BUILD}}
    - name: Upload Build
      uses: actions/upload-artifact@v3
      with:
        name: ${{ matrix.OUT_FILE_NAME}}
        path: ./dist/${{ matrix.OUT_FILE_NAME}}