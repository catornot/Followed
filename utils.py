import json
import os

class Utils:
    # @staticmethod
    # def read_levels(base_path):
    #     levels = []
    #     for i in range(len(os.listdir(base_path))):
    #         with open(base_path + "/" + str(i) + ".txt", "r") as file:
    #             data = file.read()
    #         level = []
    #         for row in data.split("\n"):
    #             level.append(list(row))
    #         levels.append(level)
    #     return levels
